import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingBottom: 60,
  },
  headerButton: {
    fontSize: 14,
    lineHeight: 22,
    color: 'white',
  },

});
export default styles;
