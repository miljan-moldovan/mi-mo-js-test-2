import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingBottom: 60,
  },
  headerButton: {
    fontSize: 14,
    lineHeight: 22,
    color: 'white',
  },
  conflictsBox: {
    marginHorizontal: 10,
    marginVertical: 10,
  },
  conflictText: {
    color: 'red',
  },
});
export default styles;
